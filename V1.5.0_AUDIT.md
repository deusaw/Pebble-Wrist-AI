# Wrist AI v1.5.0 Audit

Audit date: 2026-07-01
Scope: watch C, PebbleKit JS, Config, message protocol, storage, Timeline, ToDo & Notes, Wakeup, TTS, documentation and five-platform build.

## Result

No release-blocking defect was found by static analysis or automated integration checks. The project builds a five-platform PBW successfully.

The remaining risks require physical-device or companion-app validation and must not be represented as verified:

- Config export must still be tested from the iOS Pebble App with Chinese, emoji and a maximum-length conversation.
- ToDo & Notes list navigation, conversation restoration, seven reminder slots and the
  reserved Todoist sync Wakeup require real-watch testing.
- Flint/Duo TTS remains experimental because no Duo hardware is available.
- Core Devices currently ignores third-party Timeline developer actions, so `openWatchApp` is future-ready but not user-visible today.
- Core Devices' `insertTimelinePin` and `deleteTimelinePin` bridges have no
  completion callbacks. Wrist AI can confirm submission, not watch delivery.
- iOS may delay or fail a scheduled network sync after a phone reboot until the
  Pebble App has been opened once.

## Corrections included in the audited tree

- Fixed the Notes Complete/Reopen/Delete crash path: the watch no longer mutates
  or reloads the underlying Notes `MenuLayer` from the action flow. The phone is
  the single source of truth and returns a full `NOTE_LIST` snapshot 600 ms
  later, after the action window has closed.
- Before parsing a snapshot whose row count may shrink, the watch moves the
  Pebble SDK's selection to row zero, reloads the new data, then restores a
  clamped selection. This prevents the old last-row index from entering
  `draw_row` after a deletion.
- Note mutations are now queued in watch persistent storage from the menu
  callback and transmitted 900 ms later when the UI is idle. Failed operations
  remain queued, retry automatically, and survive a watch-app restart.
- Note commands are idempotent (`complete`, `reopen`, `delete`) so a retry
  cannot accidentally toggle a completed item back to open.
- Completing or deleting an ordinary Note no longer sends an unnecessary
  `NOTE_WAKEUP cancel` message. Wakeup cancellation is limited to Notes that
  actually have a strong scheduled reminder.
- Reopening an ordinary Note follows the same rule. Previously its separate
  rescheduling branch still sent `NOTE_WAKEUP cancel` unconditionally; this was
  the remaining crash path after Complete/Delete had been stabilized.
- Fixed timed Timeline requests such as `提醒我明天下午3点开会`. The validator now
  accepts `relative_days + local_time`, `date + local_time`, clock-only `time`
  combined with a date, and numeric `hour/minute`.
- Added a deterministic Chinese fallback that recovers today/tomorrow/day-after
  plus a clock time from the original user request if a planner omits the event
  time. Absolute ISO times without an offset remain interpreted in the phone's
  local timezone.
- Updated the Timeline planner schema with an explicit
  `relative_days: 1, local_time: "15:00"` example.
- Untimed enumerated reminders are treated as TODO Notes, not Timeline events.
  The exact request `三个提醒“洗衣服、买菜、做饭”` deterministically creates
  three Notes even if both LLM control-block paths fail. The fallback refuses
  requests containing a date or time, preventing duplicate Timeline/Note items.
- Moved the large Emery/Flint TTS PCM ring to heap allocation, removing the Emery 65,535-byte static-image failure while preserving playback capacity.
- Prevented opening a Note conversation from popping the main application window.
- Prevented a failed Timeline insertion from leaving an orphan linked ToDo & Notes item.
- Re-schedules strong reminders when a TODO is reopened or its time changes.
- Uses an all-day event's explicit reminder time for a linked strong Wakeup instead of midnight.
- Limits full active-Note context to its originating chat, preventing Note content from leaking into unrelated conversations.
- Restores a Note's original chat when Wrist AI is launched by its Wakeup; creates a replacement chat only if the original was deleted.
- Reorganized Config without changing existing control IDs or the PebbleKit configuration response schema.
- Updated the capability prompt so Wrist AI mentions ToDo & Notes when asked what it can do.
- Added serverless Todoist two-way synchronization with a durable offline
  outbox, stable Todoist task IDs, incremental Sync API reads, project scoping,
  conflict-safe idempotent completion/reopening and Homepage status.
- Todoist due dates project to Timeline; adding or changing due upserts a Pin,
  while removing due, completion and deletion use Core Devices'
  `Pebble.deleteTimelinePin`. Reopening receives a new Pin ID because Pebble
  forbids reuse after deletion.
- Added a reserved rolling sync Wakeup. Seven slots remain for strong reminders;
  a one-minute Wakeup collision shifts synchronization by two minutes.
- Config now includes Todoist enablement, token lifecycle, project selection,
  connection testing and optional scheduled synchronization.

## Automated verification

- `node --check src/pkjs/pebble-js-app.js`
- `node --check build/pkjs/pebble-js-app.js`
- Timeline regression cases: relative day plus local clock, calendar date plus
  local clock, and original-question recovery for `提醒我明天下午3点开会`
- Notes regression: `三个提醒“洗衣服、买菜、做饭”` creates exactly three TODOs,
  reports no Timeline intent, and the same pattern with a time is excluded from
  the untimed Notes fallback
- Config inline JavaScript parse and duplicate-ID check
- Notes integration: multi-create, complete, reopen, delete
- Timeline-linked ToDo & Notes and strong Wakeup message generation
- Timeline insertion failure does not persist an orphan ToDo & Notes item
- Todoist simulated integration: create/link/update/complete/reopen/delete,
  Note import, Timeline import, due addition/removal, Pin deletion, offline
  retention and missing-create-ID retention
- Todoist live API lifecycle: projects `200`, incremental sync `200`, create
  `200`, update `200`, complete `204`, reopen `204`, delete `204`
- Browser Config QA: visual layout, stored-value initialization and returned
  Todoist settings payload
- `git diff --check`
- `uv tool run --from pebble-tool pebble build`
- Basalt, Chalk, Diorite, Emery and Flint included in `build/pebble-app.pbw`

Last clean-built audited PBW:

- Path: `build/pebble-app.pbw`
- SHA-256: `b5944a04038f35bc3c0d4dfd781afe38abfe65a052717bebc324f90e20e838d2`
- Size: 423,563 bytes

Config is hosted separately and is not embedded in the PBW. Deploying `config/index.html` is therefore a separate release step.

### iOS Settings loader correction

The first v1.4.2 test PBW copied the source file directly and left
`require('opencc-js/t2cn')` unresolved. The watch binary could install, but
PebbleKit JS stopped before registering `showConfiguration`, so iOS could not
open Settings. The legacy single-file build now vendors the OpenCC UMD bundle
into the root `pebble-js-app.js` without enabling MultiJS. The final PBW is
unpacked and executed without external `require`; the test verifies Config URL
creation and a real Traditional-to-Simplified conversion.

### Theme, title and TODO audit

- Theme headers use Gothic 18 Bold in a 26px bar and are shifted down 2px.
- Theme can remain random or be fixed to seven colors, including Deep Gray.
- Explicit named TODO creation has a deterministic fallback when the model omits `NOTE_ACTIONS`.
- Config returns only dirty Note updates and deletions from a bounded URL payload.
- The final PBW passed unpacked Settings, OpenCC, TODO fallback, Config Note and theme synchronization tests.
- Config deployment commit: `eb0b116`.
- Todoist Config deployment commit: `f9cc6cf`; the deployed file hash matches
  the audited local `config/index.html`.

### ToDo & Notes / Timeline source-of-truth audit

- Renamed the user-facing local list to **ToDo & Notes** on watch, Config and
  docs. Internal protocol names remain `NOTE_*` for compatibility.
- Wrist AI-created Timeline pins are now always represented by a local
  ToDo & Notes item. Pure appointments become `event` items; actionable items
  can still be `todo`.
- ToDo & Notes items with `timeline_pin_id` delete the local Timeline pin via
  `Pebble.deleteTimelinePin` when completed/deleted, and Todoist task IDs are
  linked back to both the local item and the Timeline map.
- Todoist due tasks import as ToDo & Notes `event` items and are projected to
  Timeline. Untimed Todoist tasks import as TODO items.
- Existing Timeline map entries without a local item are reconciled into
  ToDo & Notes on the next list refresh.
- Core Devices exposes insert/delete bridges but no PKJS Timeline list/read API,
  so Wrist AI can only manage pins it created and saved locally.
- Latest test PBW: `build/Wrist-AI-v1.4.2-todo-notes-timeline-source.pbw`
  SHA-256 `c5eda185d31ab9c928338ef4bead84ad6d02e01575f70e3f72cb9a5585f5daf9`.

### Todoist post-sync crash correction

- Reproduced the control-flow distinction for an empty Todoist account and an
  empty local item store: the Note list and `Synced` status are both handled
  during startup, while the rolling OS Wakeup was first applied after the
  network sync completed.
- Wakeup cancellation/query/scheduling is no longer executed inside the
  AppMessage inbox callback. It is deferred to an app timer, coalesces repeated
  requests, and preserves an existing Wakeup when the requested time is within
  90 seconds.
- The timer is cancelled during app teardown, preventing a callback from
  touching deinitialized app state.
- Five-platform compilation, JavaScript syntax validation, PBW archive
  validation and `git diff --check` pass.
- Test PBW: `build/Wrist-AI-v1.4.2-sync-crashfix.pbw`
  SHA-256 `498d4c73820f948991daaea86c38bf727a8a14c3b1eb7fdd3d94db9ddac39104`.
- Flint real-device logging subsequently identified the exact remaining fault:
  every launch terminated after about five seconds at `PC=0x648c`, which
  resolves to newlib `strtol`.
- All watch-side decimal control parsing now uses a bounded int32 parser instead
  of `strtol`/`atoi`. This covers sync Wakeups, Note Wakeups and model indices,
  rejects malformed/overflowing values, and removes about 1.1 KB from Flint RAM
  footprint.
- The corrected PBW was installed over Developer Connection and remained open
  beyond the former deterministic crash window with no further app fault.

### TTS real-device transport audit

- Added low-overhead session metrics for encoded/received/played bytes, ring
  peak, flow-control transitions, underruns and speaker FIFO short writes.
- The same 204,625-byte, four-sentence reply produced one underrun with the
  Pebble iOS app foregrounded, but 16 underruns when it was backgrounded.
  Every underrun occurred with eight 350-byte packets already queued, proving
  that iOS per-message callback throttling—not synthesis speed or ring size—was
  the limiting factor.
- Raw PCM quality is retained. TTS transport now uses 1,200-byte packets with
  at most two packets in flight; the 2,400-byte maximum remains below Flint's
  3,788-byte ring headroom above its pause watermark.
- Repeating the same test with the Pebble app backgrounded transferred and
  played all 204,625 bytes with zero underruns, zero overflows and zero
  PAUSE/RESUME cycles. Subjective playback was reported as smooth.

### LOUD1 and ToDo & Notes archive audit

- Emery high-quality TTS uses 16 kHz μ-law transport and native 16-bit speaker
  playback. Speech mastering applies DC removal, a soft noise gate, 2.5:1
  compression and peak limiting. Menu 100% maps to hardware 50%; Flint remains
  on the established 8 kHz path.
- Emery transport uses 3,000-byte packets with smoothed 130 ms pacing and
  27,000/21,000 flow-control watermarks.
- Todoist due values are all-day only when exactly `YYYY-MM-DD`. Datetime
  strings carried in `due.date` remain timed Timeline events.
- Timeline-linked watch rows explicitly show `Timeline` plus their local time
  or `All day`.
- Active items and completed Archive entries have independent 50-item
  retention. The watch shows active items plus one compact Archive entrance;
  archived rows live in a separate native menu window.
- BACK from a conversation selected through the UP menu returns to that menu
  instead of exiting to Pebble OS.
- Retention/date-label tests, JavaScript syntax validation, `git diff --check`,
  five-platform compilation and PBW archive validation pass.

### UX1 foreground sync, controls and TTS finishing audit

- Todoist continues to sync on launch and after local mutations. While Wrist AI
  remains open, a watch-side five-minute timer now requests incremental sync;
  recording, model work, TTS and pending Note commands defer it by 30 seconds.
- Foreground sync failures are handled as sync retries and cannot fall through
  to the chat-send error path. A running Todoist sync remains single-flight.
- `Syncing Todoist`, `Synced Todoist`, offline and failure state are drawn as a
  small gray line below the conversation title. They no longer replace it.
- Background Wakeup choices remain independent. Config explains the difference
  between five-minute in-app sync and optional 30/60/180-minute app Wakeups.
- Emery mastering now uses a smoothed envelope gate and compressor, a mild
  high-frequency smoother and six-millisecond fades at independently
  synthesized sentence boundaries. Hardware volume remains capped at 50%.
- The UP menu includes a `Controls` reference rendered with the same compact
  themed header, system fonts, white content background and selection inversion
  used by the rest of the watch UI.
- Audit found and fixed two additional state bugs: Archive now reloads safely
  after returning from Complete/Reopen/Delete, and a failed foreground sync
  cannot be reported as a failed LLM message.
- Test badge: `UX1`.
- Final UX1 PBW SHA-256:
  `66569311507928fea8f6107e608eeb4a24c45e30e7fdb5040b3ce7ff41653789`.

### 150A v1.5.0 finalization audit

- Release metadata, Config cache-busting, Config copy and the model capability
  disclosure now consistently report v1.5.0.
- `CONTROLS_VISIBLE` was appended as message key `10041`; all previous key
  numbers and the app UUID remain unchanged.
- Controls visibility defaults on. Config can disable it, and every following
  UP-menu row is calculated dynamically rather than relying on fixed indices.
- The Controls reference ends with `New-user tutorial / Hide it in Config`.
- Todoist disabled sends an empty status; enabled without a token sends
  `Set up Todoist`; configured sync uses the normal live states.
- A stale background sync Wakeup safely closes after disabled or incomplete
  Todoist configuration is reported.
- A clean build was required after adding the message key because Pebble's
  incremental build retained an old generated C header. Clean five-platform
  compilation regenerated a matching C/JS key map.
- Test badge: `150A`.
- Final v1.5.0 test PBW SHA-256:
  `dba125156f45ee642116e2bb250af2752f5074c75f52ed965c4e078db0c5793f`.
- JavaScript syntax, Config script/static-ID checks, four dynamic menu-index
  combinations, append-only message metadata, `git diff --check`, all five
  platform builds, PBW archive validation and embedded PBW metadata pass.

### 150B Memory routing and Config audit

- Stable personal-memory intent is detected separately from tasks and Notes.
  Birth dates, identity, durable preferences, routines, accessibility needs and
  long-term goals bypass NOTE_ACTIONS even if the main model emits one.
- A successfully parsed usable `MEMORY_UPDATE` is applied before Notes and
  Timeline routing. Missing, malformed, empty or sensitive-only updates do not
  suppress the dedicated Memory planner.
- The planner is constrained to stable user-provided facts and exact existing
  keys for removal. An explicit valid birth date also has a deterministic local
  fallback, covering `记住我是1996年11月16日出生`.
- Memory writes cap each model operation at three upserts/removals and the
  document at 40 items / 12 KB. Duplicate keys merge, oldest entries are pruned
  only when necessary, and passwords, tokens, API keys and financial
  identifiers are filtered.
- Config receives the canonical Markdown in its bounded URL hash, displays it
  in a monospaced editor, and returns it only when changed.
- Config blocks malformed lines, more than 40 items or more than 12 KB. The
  phone validates again; malformed edits preserve the old document while a
  blank/header-only edit deliberately clears it.
- Test badge: `150B`.
- Destructive-path tests cover malformed-edit preservation, intentional clear,
  three-operation caps, sensitive-entry filtering, duplicate-key merging and
  malformed control-block fallback.
- Final 150B PBW SHA-256:
  `7242c3f92a37779ee3a1d31365d27d3976ed884dde517d2e1a86d04fee71a5cf`.

### App Store release assets

- Replaced the v1.4.0-era Description with separate English and Chinese
  v1.5.0 plain-text files covering Memory, ToDo & Notes, natural Timeline,
  Todoist, health, TTS, privacy, setup and current button controls.
- Added separate English and Chinese plain-text Release Notes for direct
  dashboard pasting, plus the complete bilingual Markdown release history.
- Generated and visually inspected a 720×320 opaque PNG banner using the Wrist
  AI violet/orange palette, Pebble-style pixels, watch product composition and
  the exact slogan `Your Pebble will be better with Wrist AI !!`.
- Removed the on-watch test badge for the formal build.

### Final release audit

- Performed a clean five-platform build. Free runtime heap remains about
  28.9KB on Flint, 94.3KB on Emery and 34.6KB or more on legacy targets.
- All raw platform binaries are below 33KB; the former 65,535-byte image limit
  is not approached.
- Reverse-inspected the formal PBW: version `1.5.0`, unchanged UUID, appended
  `CONTROLS_VISIBLE=10041`, single-file bundled PKJS, Memory planner, Config
  Memory payload and cache-buster are present.
- Confirmed the formal build has an empty test-badge string.
- Re-ran Memory destructive-path tests, TTS peak/silence/boundary tests,
  JavaScript syntax checks, Config static-ID checks, package metadata checks,
  archive validation and secret-pattern scanning.
- Tested Config in a real browser at a 390×844 mobile viewport: no horizontal
  overflow, no console warnings/errors, Memory editor 338×190, correct Memory
  content/status, invalid-line warning and recovery, Controls state, Todoist
  missing-token state, theme and TTS quality all render correctly.
- Formal PBW: `build/Wrist-AI-v1.5.0.pbw`
- Formal PBW SHA-256:
  `e0f20dd329827dcd1deb76f4316e6e95bd055ffaf20e03167fce0a174dcd3986`.

## Storage and limits

| Data | Location | Limit |
|---|---|---:|
| Conversations | Phone `localStorage` | 20 chats |
| Messages | Phone `localStorage` | 20 messages per chat |
| Memory.md | Phone `localStorage` | 40 items / 12 KB |
| Active ToDo & Notes | Phone `localStorage` | 50 items |
| Completed Archive | Phone `localStorage` | 50 items |
| Watch item snapshot | AppMessage / watch heap | 20 active + 10 recent archived |
| Strong reminders | Watch persistence / Wakeup API | 7 slots |
| Todoist sync Wakeup | Watch persistence / Wakeup API | 1 rolling slot |
| Todoist offline outbox | Phone `localStorage` | Durable / compacted |
| Todoist incremental token | Phone `localStorage` | 1 |
| Visible reply | Phone truncation / watch buffer | 1,800 UTF-8 bytes |
| Timeline launch map | Phone `localStorage` | 40 entries |

## Release decision

The code is suitable for a v1.5.0 test build. Public release should wait for
the remaining real-device checks, especially iOS Config persistence, Emery TTS
regression, five-minute foreground Todoist sync and hidden-Controls menu
indexing.
